{
  "extends": ["react-app", "standard"],
  "rules": {
    "comma-dangle": ["error", "always-multiline"],
    "no-unused-vars": ["warn"],
    "no-var": ["error"]
  },
  "overrides": [
    {
      "files": ["**/*.ts?(x)"],
      "rules": {
        "semi": "error"
      }
    }
  ]
}
